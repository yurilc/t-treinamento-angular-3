import { DificuldadePipe } from './dificuldade.pipe';

describe('DificuldadePipe', () => {
  it('create an instance', () => {
    const pipe = new DificuldadePipe();
    expect(pipe).toBeTruthy();
  });
});
